# Secrets — Razorpay test-mode keys live here, never commit
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Simulation artifacts
*.db
*.sqlite
*.sqlite3
runs/
logs/
.cache/

# Node / replay UI
node_modules/
dist/
build/

# Editors & OS
.DS_Store
.vscode/
.idea/

# Third-party Claude Code skill plugin cache. 94 MB of someone else's
# files; nothing here is part of the exchange.
.agents/

# Design detector's local hook cache. The ignore decisions in
# .impeccable/config.json are kept; the cache is per-machine.
.impeccable/hook.cache.json
